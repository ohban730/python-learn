# テキスト生成タスク（Baseモデル と Instruct/Chatモデル の違い）

Hugging Face の `pipeline("text-generation")` において、「後続する文字列を単に補完するだけなのか？指示に従って文章を作成できないのか？」という疑問について解説します。

---

## 1. 結論：使用する「モデルの種類」によって挙動が変わります

すべてのLLMは、根本的な仕組みとしては「与えられたテキストの続きを予測して補完する」という動きをしています。

しかし、モデルがどのような追加学習を施されているかによって、振る舞いが決定的に異なります。

* **ベースモデル (Base Model / Foundation Model)**
  * **単に続きを補完するだけ**です（指示には従いません）。
* **指示追従モデル (Instruct Model / Chat Model)**
  * ユーザーの**指示（プロンプト）に従って回答を作成**します。

---

## 2. 挙動の違いの具体例

例えば、モデルに対して `Pythonのメリットを説明してください。` と入力した場合の挙動の違いです。

### ① ベースモデルの場合（自動補完）
インターネット上の文章をただ真似るように学習しているため、「質問文の次には、別の質問文や関連テキストが続く可能性が高い」と判断し、以下のように勝手に「続き」を補完します。

> **入力**: `Pythonのメリットを説明してください。`
> **出力**: `また、Javaのメリットについても解説し、C++との違いを比較してください。さらに、初心者におすすめのプログラミング言語についても記述してください。`

* **理由**: インターネット上にある「プログラミング勉強会のQ&Aシート」や「ブログの目次」などのテキストパターンをそのまま補完してしまっているためです（指示に従うという概念を学習していません）。

### ② 指示追従（Instruct / Chat）モデルの場合（指示に従う）
ベースモデルに対して、「指示（プロンプト）」と「それに対する人間らしい回答」のペアを大量に与えて追加訓練（**指示チューニング / Instruction Tuning**）したモデルです。

> **入力**: `Pythonのメリットを説明してください。`
> **出力**: `Pythonの主なメリットは以下の3点です。1. シンプルで読みやすい文法 2. 豊富なライブラリ（AIやデータ分析に強い） 3. 活発なコミュニティ...`

* **理由**: 「ユーザーが質問や指示をしてきたら、その要求に答える文章を書き出す」というパターンを叩き込まれているため、補完の仕組みを使いつつも「回答文」を出力できます。

---

## 3. Hugging Face の pipeline で指示に従わせる方法

Hugging Faceの `pipeline("text-generation")` でデフォルトモデルを使うと、前述の「ベースモデル」が選ばれるため、単なる自動補完になってしまいます。

指示に従わせるためには、モデル名に `-Instruct` や `-chat` とついたモデルを明示的に指定し、さらに **チャットテンプレート（Chat Template）** を使用して対話形式で入力します。

### コード例（指示に従うテキスト生成）

```python
from transformers import pipeline

# 1. 指示追従（Instruct）モデルをロードする
# 例: Qwen2.5の軽量な指示追従モデル
generator = pipeline(
    "text-generation", 
    model="Qwen/Qwen2.5-1.5B-Instruct", 
    device_map="auto"
)

# 2. チャット形式のテンプレートで指示（プロンプト）を組み立てる
messages = [
    {"role": "system", "content": "あなたは優秀なAIアシスタントです。質問に対して簡潔に回答してください。"},
    {"role": "user", "content": "Pythonのメリットを3つ、箇条書きで教えて。"}
]

# 3. テキスト生成を実行
outputs = generator(messages, max_new_tokens=150)

# 生成された結果の表示
print(outputs[0]["generated_text"][-1]["content"])
```

### なぜこれで指示に従うのか？
上記の `messages` 形式で入力を渡すと、モデル内部で自動的に `[INST]` や `<|im_start|>` といった「AIへの指示の境界線を示す特殊な記号」が裏で付与されます。
モデルはその特殊な記号を認識し、「ここからはユーザーの指示であり、私は回答を生成（補完）しなければならない」と理解して動くため、指示通りの文章を作成してくれます。
